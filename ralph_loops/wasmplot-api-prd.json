{
  "project": "WasmPlot Clean Makie API",
  "description": "Make WasmPlot Makie-like API work end-to-end inside Therapy.jl @island effects compiled to WASM",
  "stories": [
    {
      "id": "API-001",
      "title": "Two-pass autodiscovery fixes stubbing",
      "repo": "WasmTarget.jl",
      "status": "done",
      "completedNote": "Two-pass autodiscovery in _autodiscover_closure_deps! fixed stubbing (25→3 warnings, only error paths remain)"
    },
    {
      "id": "API-002",
      "title": "Math function stubbing",
      "repo": "WasmTarget.jl",
      "status": "done",
      "completedNote": "Same autodiscovery fix covers math functions"
    },
    {
      "id": "API-003",
      "title": "Therapy 'Has effect: false'",
      "repo": "Therapy.jl",
      "status": "done",
      "completedNote": "Investigation proved effect IS compiled and wired correctly. The diagnostic check `contains(result.js, 'effect')` was misleading — WASM effects run through funcref table + _rt_flush, not explicit JS 'effect' code. Verified: (1) _effect_0 exported at func index 72, (2) 21 canvas2d imports present, (3) funcref table has 2 entries, (4) _rt_flush(BigInt(3)) triggers all effects, (5) WASM validates clean with wasm-tools, (6) effect body calls canvas2d.clear_rect, fill_rect, set_fill_rgb etc. API-001/002 autodiscovery fixes resolved the underlying compilation issue."
    },
    {
      "id": "API-004",
      "title": "End-to-end browser verification",
      "repo": "Therapy.jl",
      "status": "done",
      "description": "Serve the InteractivePlot island in a browser and verify canvas rendering works — freq buttons change the sine wave",
      "completedNote": "Full end-to-end browser verification via Puppeteer headless Chrome. Built static site (all 10 islands compile), served with correct base_path, verified in real browser: (1) WASM validates (65,415 bytes, 21 canvas2d imports), (2) InteractivePlot island hydrates successfully, (3) Canvas renders blue sin wave — pixel sampling confirms rgb(60,130,246) data line + gray grid, (4) + button increments freq 3→4 with full canvas redraw (320 canvas2d calls), (5) - button decrements freq back, (6) All 11 islands on page hydrate with zero WASM errors and zero page errors. Node.js headless test also confirmed: effect produces 223 line_to calls per render (200 data points + grid), clear_rect→fill_rect→grid→data rendering sequence correct. WasmPlot tests 69/69 + 11/11 WASM compile pass."
    }
  ]
}
