{
  "name": "therapy-jl",
  "version": "1.0.0",
  "description": "Browser tests for Therapy.jl framework",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0"
  }
}
